name: CI

on:
  push:
    branches:
    - master
    - release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Lint and build
      run: |
        npm run lint
        npm run build
